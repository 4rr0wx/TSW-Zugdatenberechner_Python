version: "3.8"

services:
  app:
    image: ghcr.io/OWNER/TSW-Zugdatenberechner_Python:latest # replace OWNER with your GitHub username or org
    container_name: tsw-zugdatenberechner
    restart: unless-stopped
    ports:
      - "8080:8080"
